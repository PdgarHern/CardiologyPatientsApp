default: &default
  adapter: postgresql
  encoding: unicode
  
  host: ec2-63-35-156-160.eu-west-1.compute.amazonaws.com
  port: "5432"
  database: d6mqkaaq6jsvis
  username: opyzqabfatgqzo
  password: a7832ec475f86c51f98ee5e3bfc67b60cdb3cb916ca038717e2e537a3ec79217

  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: cardiology_backend_development

  host: <%= ENV['host_postgres'] %>
  port: <%= ENV['port_postgres'] %>
  username: <%= ENV['username_postgres'] %>
  password: <%= ENV['password_postgres'] %>

  # Schema search path. The server defaults to $user,public
  #schema_search_path: myapp,sharedapp,public

  # Minimum log levels, in increasing order:
  #   debug5, debug4, debug3, debug2, debug1,
  #   log, notice, warning, error, fatal, and panic
  # Defaults to warning.
  #min_messages: notice

test:
  <<: *default
  database: cardiology_backend_test

production:
  <<: *default
